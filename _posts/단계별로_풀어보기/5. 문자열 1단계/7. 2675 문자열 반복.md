<div class="block_white">

## **설명**

<div class="block_yellow">

### **입력**

- T: 1,000 이하의 테스트 케이스 수

- (R, S)[]: T 개의 테스트 케이스

    + R: 반복할 횟수

    + S: 변환할 문자열

</div>

<div class="block_green">

### **출력**

- 문자열의 각 문자를 R 만큼 반복해서 한 줄 씩 출력한다.

- R=2, S=abc 이면 aabbcc 를 출력한다

</div>

</div> <!-- 설명 -->


<div class="block_grey">

## **소스 코드**

<div class="block_green">

### **Node**

``` js
// 입력
const fs = require('fs')
const inputArr = fs.readFileSync(0, 'utf8').toString().trim().split('\n')

// 입력 파싱
const T = +inputArr[0] // parseInt 보다 이게 더 빠르다. 안 써도 되긴 한다.
const testCases = inputArr.slice(1)

// 테스트 케이스마다 작업 수행
testCases.forEach(input => {

  // 입력을 둘로 나눈다.
  let [R, S] = input.split(' ')

  // 반복횟수 R 을 숫자로 변환한다.
  R = +R // parseInt 보다 이게 더 빠르다고 한다.

  let resultCase = ''

  for (let i = 0; i < S.length; i++) {
    const c = S.charAt(i)
    resultCase += c.repeat(R)
  }

  process.stdout.write(`${resultCase}\n`)
})
```

<br />

<div class="block_white tp_80">

### **공통**

- #### 리눅스 환경에서 이상한 문자가 추가되어 문자열에 들어가는것을 방지하고자 trim 을 썼다.

- #### 대용량 입출력을 연습하기 위해 process.stdout.write 함수를 쓰는 습관을 들인다.

- #### **입력 부분을 로컬에서도 싫행 가능하게 바꿨다**

</div>

<div class="block_grey tp_80">

### **특이점**

- #### **드디어 로컬에서도 Node 를 돌려볼 수 있게 됬다.**

    + 그전까지는 테토남처럼 머릿속에서만 버그를 고쳤다.

    + 이전까지 쓰던 입력방식은 Window 에서 작동하지 않았다.

    + 하지만 바뀐 방식은 Window 에서도 작동한다.

    + <s>휴... 나중에 난이도 높아졌을때 난리날뻔...</s>

- #### 변수 T 는 장식용으로 두고 forEach 문을 사용함

</div>

</div> <!-- 소스 코드/ Node -->

<div class="block_blue">

### **Python**

``` python
import sys

input = sys.stdin.readline
output = sys.stdout.write

T = int(input())

while T > 0:
  R, S = input().strip().split()
  R = int(R)

  resultS = ''.join([s * R for s in S])

  output(f"{resultS}\n")

  T -= 1
```

<br />

<div class="block_white tp_80">

### **공통**

- #### Python 역시 대량 입출력에 익숙해지기 위해 저 함수들을 쓴다.

- #### output 은 format 을 지정하여 커스터마이징이 용이하도록 한다.

</div>

<div class="block_grey tp_80">

### **특이점**

- #### 출력할 문자열을  join 함수로 생성

- #### JS 의 join 과 기능이 아주 많이 다르다는걸 유념해야함.

</div>

</div> <!-- 소스 코드/ Python>

</div> <!-- 소스 코드 -->