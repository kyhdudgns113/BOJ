<div class="block_white">

## **설명**

- 10 미만의 두 자연수 A, B 를 입력받고 A - B 를 출력한다.

</div>

<div class="block_grey">

## **소스 코드**

<div class="block_green_transparent">

### **Node**

``` js
const fs = require('fs')
const input = fs.readFileSync('/dev/stdin').toString().split(' ')
let [a, b] = input

a = parseInt(a)
b = parseInt(b)

process.stdout.write((a - b).toString())
```

<br />

- #### parseInt 를 안해도 숫자로 자동변환 하기는 한다.

- #### 가독성과 추후 실수 방지를 위해서 습관을 들여놓자는 의미로 parseInt 를 했다.

</div>

<div class="block_blue_transparent">

### **Python**

``` python
import sys

input = sys.stdin.readline
output = sys.stdout.write

A, B = map(int, input().split())

print(A - B)
```

</div>

</div>